{
  "name": "music-room",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\"",
    "lint": "cd backend && npm run lint && cd ../mobile && npm run lint"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0"
  },
  "lint-staged": {
    "*.{ts,tsx}": ["prettier --write"],
    "*.{json,md}": ["prettier --write"]
  }
}
